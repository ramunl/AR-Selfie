package us.cyberstar.domain.internal.manger.arScene
import us.cyberstar.domain.internal.model.PostNode

class PostNodePositionGenerator {

    fun giveMePositionForAPost(postNode: PostNode) {
        val postSize = postNode.size
       /* if (postNode.isQuick()) {
            //get horizontal position
        } else {
            //get vertical position
        }*/
    }
}